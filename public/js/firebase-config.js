/*
  Firebase configuration placeholder.
  Replace the contents of `window.FIREBASE_CONFIG` with your project's config
  from the Firebase console. Example:

  window.FIREBASE_CONFIG = {
    apiKey: "...",
    authDomain: "...",
    databaseURL: "https://<PROJECT>.firebaseio.com",
    projectId: "...",
    storageBucket: "...",
    messagingSenderId: "...",
    appId: "..."
  };

  This file is intentionally a simple global so the dashboard can initialize
  without requiring a build step.
*/

window.FIREBASE_CONFIG = window.FIREBASE_CONFIG || {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME",
  databaseURL: "REPLACE_ME",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
